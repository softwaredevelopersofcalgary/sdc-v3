# When adding additional environment variables, the schema in "/src/env.mjs"
# should be updated accordingly.

# Prisma
# https://www.prisma.io/docs/reference/database-reference/connection-urls#env
DATABASE_URL=''
# DATABASE_URL='postgresql://root@localhost:26257/sdc?application_name=%24+cockroach+sql&connect_timeout=15&sslmode=disable'

# Next Auth
# You can generate a new secret on the command line with:
# openssl rand -base64 32
# https://next-auth.js.org/configuration/options#secret
NEXTAUTH_SECRET=""
NEXTAUTH_URL=""

# Next Auth Github Provider
GITHUB_CLIENT_ID=""
GITHUB_CLIENT_SECRET=""

# Randomly generated secret that must match what is put in the cron call in Vercel.
CRON_SECRET=

# Set up in GitHub, the token will be used to call the API to get information from repo's and users.
# Utilized with the contributors pages and will get info as the users token that is generated.5
GITHUB_TOKEN=

# Polling interval (ms) for the events list page. Defaults to 10000 (10s) if not set.
# Can be adjusted in Vercel dashboard without a PR — requires a redeploy to take effect.
NEXT_PUBLIC_EVENTS_POLL_INTERVAL_MS=10000
